import { StringExtenderPipe } from './string-extender.pipe';

describe('StringExtenderPipe', () => {
  it('create an instance', () => {
    const pipe = new StringExtenderPipe();
    expect(pipe).toBeTruthy();
  });
});
